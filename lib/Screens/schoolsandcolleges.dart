import 'package:flutter/material.dart';

class SchoolsAndColleges extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Schools and colleges page'),
    );
  }
}
